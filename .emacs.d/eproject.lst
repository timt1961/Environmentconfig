;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list
      '(("eproject" "/home/utt/emacs/python-for-emacs/extensions/eproject")))

(setq prj-last-open nil)

(setq prj-frame-pos
      '((top . 26)
        (left . 1239)
        (width . 80)
        (height . 54)))

